machine:
  services:
    - docker
    - redis

dependencies:
  override:
    - docker info
    - docker build --rm=false -t nikhila05/micro-finance .

test:
  override:
    - python manage.py test

deployment:
  hub:
    branch: master
    commands:
      - docker login -e nikhila@micropyramid.com -u nikhilamergu -p nikhila05@
      - docker push nikhila05/micro-finance
